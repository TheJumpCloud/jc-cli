# TUI Backlog

## Completed

| Feature | Description | Phase |
|---------|-------------|-------|
| TUI search | Full-text search bar on list screens (POST `/search/` endpoints) | Phase 3 |
| TUI help overlay | `?` key shows contextual key binding cheat sheet | Phase 4 |
| TUI export | Copy full JSON or export table as CSV from list screen | Phase 5 |
| TUI bookmarks | Pin favorite resources for quick access from home | Phase 6 |
| Insights event detail | Drill into events + AI explanation (`x` key) | v1.2.0 |
| TUI CRUD | Create (`n`), edit (`E`), delete (`d`) from list/detail screens | v1.3.0 |

## Pending

_No items — all planned features implemented._
